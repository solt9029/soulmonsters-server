projects:
  soulmonsters-server:
    schemaPath: prisma/generated/schema.graphql
    extensions:
      endpoints:
        default: ${env:PRISMA_ENDPOINT}
      codegen:
        - generator: prisma-binding
          language: typescript
          output:
            binding: src/prisma/generated/prisma.binding.ts
